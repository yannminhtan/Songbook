rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to the "apks" folder
    match /apks/{allPaths=**} {
      allow read;
      allow write: if false; // Disallow write access for everyone
    }
  }
}
